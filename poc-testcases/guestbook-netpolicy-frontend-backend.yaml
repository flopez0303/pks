kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-frontend-backend
spec:
  podSelector:
    matchLabels:
      app: redis
  ingress:
  - from:
      - podSelector:
          matchLabels:
            app: guestbook
